{
  "name": "@explorer/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@explorer/chain": "workspace:*",
    "@explorer/config": "workspace:*",
    "@explorer/db": "workspace:*",
    "drizzle-orm": "^0.33.0",
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "tsx": "^4.19.1",
    "vitest": "^2.1.1"
  }
}
